<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Вакансия</title>
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  <style>
    body { 
      font-family: Arial, sans-serif; 
      padding: 20px; 
      background: #f5f5f5;
      color: #333;
    }
    h1 { 
      color: #1a73e8; 
      font-size: 1.4em;
    }
    p {
      font-size: 1.1em;
    }
    .btn {
      background: #1a73e8; 
      color: white; 
      padding: 12px; 
      border: none; 
      border-radius: 8px; 
      font-size: 1em; 
      margin-top: 20px; 
      cursor: pointer;
    }
  </style>
</head>
<body>
  <h1 id="title">Загрузка...</h1>
  <p><strong>Компания:</strong> <span id="company"></span></p>
  <p><strong>Зарплата:</strong> <span id="salary"></span></p>
  <p><strong>Ссылка на HH:</strong> <a id="link" href="#" target="_blank">Открыть</a></p>

  <button class="btn" onclick="closeWebApp()">Закрыть</button>

  <script>
    // Получаем данные из Telegram
    const initData = Telegram.WebApp.initData;
    const data = new URLSearchParams(initData);

    // Подставляем данные в страницу
    document.getElementById("title").textContent = data.get("title") || "Без названия";
    document.getElementById("company").textContent = data.get("company") || "Не указано";
    document.getElementById("salary").textContent = data.get("salary") || "Не указана";

    const link = document.getElementById("link");
    link.href = data.get("url") || "#";

    // Говорим Telegram, что всё готово
    Telegram.WebApp.ready();

    // Функция закрытия окна
    function closeWebApp() {
      Telegram.WebApp.close();
    }
  </script>
</body>
</html>